# converters/ — convertitori di contenuto

> **Disambiguazione**: questa cartella contiene i **convertitori di
> contenuto**; l'automazione DM (prep/post sessione, recap, mappe, CLI
> `dm.py`) vive in **`scripts/`** — vedi `scripts/README-automation.md` e
> ADR-0002/ADR-0011 in `plans/adr/`. (Rinominata da `Script/`, ADR-0011.)

| Tool | Cosa fa |
|---|---|
| `pdf-to-md-engine/` (+ `tools/md2pdf.sh`) | PDF → markdown (e ritorno md → PDF) |
| `Html_to_markdown/` | HTML → markdown |
| `Image-to-webp/` | immagini → webp |
