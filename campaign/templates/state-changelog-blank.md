# Storico di `state.md` — append-only

> Registro cronologico dei cambiamenti di stato, una voce per data.
> **Append-only**: si aggiunge in fondo, non si cancella e non si riscrive.
> `state_apply` ci appende la riga di fine sessione dentro la regione marcata.

---

```
1970-01-01  Sessione 0 — gruppo nuovo. Stato inizializzato dai template
            (scripts/new-campaign-group.sh).
```
